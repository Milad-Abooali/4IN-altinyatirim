<?php

$permits = new stdClass();

// Screen - Home
$permits->home = array(
    '*'   => array(
        'view'      => true
    )
);

// Screen - Guest
$permits->guest = array(
    '*'   => array(
        'view'      => true
    )
);

// Screen - User
$permits->user = array(
    '*'   => array(
        'view'      => true
    )
);

// Screen - Transaction
$permits->transaction = array(
    '*'   => array(
        'view'      => true
    )
);

// Screen - Trade
$permits->trade = array(
    '*'   => array(
        'view'      => true,
    )
);

// Screen - Chart
$permits->chart = array(
    '*'   => array(
        'view'      => true,
    )
);

// Screen - AI
$permits->ai = array(
    '*'   => array(
        'view'      => true,
    )
);

// Screen - Info
$permits->info = array(
    '*'   => array(
        'view'      => true,
    )
);